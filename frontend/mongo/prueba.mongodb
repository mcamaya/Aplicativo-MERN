use("servitecaBga");

db.facturas.aggregate([
  { $match: { _id: "5jIQogjaProbe5TGq9FQz" } },
  { $unwind: "$productosIds" },
  {
    $project: {
      _id: 0,
      totalVenta: {
        $multiply: ["$productosIds.cantidad", "$productosIds.precio"],
      },
    },
  },
]);
